# Observability

**Runtime status:** Target architecture only.

Prometheus, Grafana and OpenTelemetry remain planned production-oriented components and are not part of the executable MVP runtime.

Current validation relies on explicit command output, MLflow experiment metadata, FastAPI health checks, Ruff and pytest integration tests.
