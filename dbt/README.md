# dbt Core

**Runtime status:** Target architecture only.

The executable MVP builds Silver, Gold and Metrics directly with deterministic DuckDB SQL in `scripts/data/build_silver_gold.py`.

The repository retains dbt scaffolding because dbt remains part of the target production-oriented architecture.

No claim is made that dbt currently runs the MVP transformations.
